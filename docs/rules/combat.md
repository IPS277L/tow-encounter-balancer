# Бой и атаки

Источник: `BOOK-PLAYER-GUIDE`, преимущественно страницы 111–120. Статус: правила атаки, базовый порядок раунда, бюджет действий, обе фазы Run и обе дальности Charge `implemented` в K1; эффекты большинства остальных действий и завершение боя ещё требуют нового battle loop.

## RULE-COMBAT-001 — раунды, стороны и ходы

Каждый участник получает один ход в каждом раунде. По умолчанию сначала действует сторона игроков и союзников, затем противники. Порядок участников внутри своей стороны выбирается заново каждый раунд; один участник полностью завершает ход до начала следующего.

Источник: страница 112.

## RULE-COMBAT-002 — основная экономика действий

Обычно участник выполняет одно боевое действие за ход и допустимые incidental actions, включая одно бесплатное перемещение. Fate или способности могут дать второе действие, но действий не может быть больше двух. Одно и то же действие нельзя повторять, а второй action не может создавать вторую атаку: поэтому Charge и обычный Attack нельзя совместить в одном ходу. GM может разрешить два разных Improvise, если они используют разные подходы.

Источник: страницы 111, 112, 116.

## RULE-COMBAT-003 — засада

При успешной засаде противники действуют первыми, и этот порядок сохраняется. В первом раунде застигнутые врасплох участники не могут Oppose атаки. Awareness и подходящие Lores могут предотвратить этот эффект.

Источник: страница 112.

## RULE-COMBAT-004 — действия

Базовый список включает Aim, Attack, Help, Improvise, Manoeuvre и Recover. Charge является вариантом Manoeuvre и включает перемещение с последующей атакой.

Источник: страницы 116–118.

Подробности:

- Aim — Awareness Test; каждый success даёт `+1d` следующей ranged attack по выбранной цели, если между ними не было другого action;
- Help выполняет собственный Test релевантного Skill и даёт союзнику по `+1d` за success на связанную upcoming Test; другой Skill помогающего допустим по решению GM;
- Manoeuvre выбирает Run, Charge, Move Quietly или Move Carefully;
- Improvise применяет подходящий Skill/spell/Ability, но только defeat или Defenceless гарантированно нейтрализует threat;
- Recover снимает Staggered/Prone, уменьшает Miscast Pool на один die и позволяет взаимодействовать с предметом рядом с врагом; вместо всех этих выгод можно Treat Wound либо Test снятие condition.

Для Skill Improvise GM заранее подтверждает конкретный подход, Test и допустимый эффект. Текущий исполняемый срез поддерживает только прямые книжные примеры Prone и Distracted: basic Test требует хотя бы одного success, opposed Test — победы инициатора. Успех создаёт source-aware application request со связью с action, Test, actor, target и stable GM approval ID; провал всё равно завершает action, но эффекта не создаёт. Отдельная application-фаза принимает актуальные `ConditionState` и immunity snapshot цели, переиспользует общий Condition reducer и сохраняет `blocked`/`was_already_present`. До появления battle aggregate вызывающая сторона передаёт immutable список уже потреблённых application IDs; один request применяется ровно один раз. Staggered и атакующие импровизации обязаны использовать специализированные pipelines.

Ability Improvise подключается только специализированными контрактами конкретных способностей. Troll Vomit использует stable approach `RULE-NPC-019:troll-vomit`, отдельный actor Ability snapshot и одну вражескую Staggered-цель на Close Range. Troll Hag Swamp Breath использует `RULE-NPC-020:swamp-breath`, запрещает Staggered действующей и применяет Endurance Hazard (3) ко всем существам выбранной Zone на Medium Range. Forest Dragon Soporific Breath использует `RULE-NPC-018:soporific-breath`, то же ограничение Staggered и Zone selection, но свой Endurance Hazard (2) с Wound → Drained и повторным Drained → Defenceless; подтверждённое наследование Ability Wood Elf передаётся в том же actor snapshot. Zone actions используют точный placement order, включая союзников и действующего. Это Hazard actions, а не Attack actions; receipt появляется только после одиночного либо полного Zone result. Зарезервированный, но неисполненный Ability Improvise блокирует окончание хода наравне с Skill и spell.

### Реализация порядка хода и action budget в K1

`CombatRoundState` хранит номер раунда, снимок участников двух сторон, постоянный порядок сторон, завершивших ход участников и не более одного активного `CombatTurnState`. Внутри текущей стороны actor выбирается внешней policy в любом порядке; следующая сторона открывается только после завершения всех ходов текущей. При переходе раунда orchestration передаёт новый снимок допущенных участников, а порядок сторон сохраняется, включая перевёрнутый порядок после засады.

`reserve_combat_action_slot` только проверяет и резервирует `CombatActionSlot`: первый слот обычный, второй обязан ссылаться на Fate либо конкретный Rule ID способности, третьего слота нет. Резолвер не расходует Fate и сам не исполняет Aim/Attack/Help/Improvise/Manoeuvre/Recover. `CombatActionDeclaration.produces_attack` учитывает обычный Attack, Charge и явно помеченный атакующий Improvise, поэтому скрытая вторая атака отклоняется независимо от формы действия. `ImproviseKind` явно различает Skill, spell и Ability; два Improvise требуют явного `allows_second_improvise` от GM policy и разных stable approach ID.

Специализированный Aim executor связывает active actor, вражескую цель, зарезервированный `CombatActionKind.AIM` и Awareness `TestRequest`. Действие завершается при любом результате Test и создаёт неизменяемый `AimBonusSnapshot`, где каждый success равен одному bonus die. Snapshot не привязан к текущему turn и поэтому может сохраниться до более позднего хода.

Отдельная граница `resolve_aim_follow_up` обязана получить именно следующее действие владельца snapshot. Shooting либо Throwing Attack по выбранной цели получает обычный `DiceModifier`, который подчиняется общему пределу пула; нулевой бонус считается использованным без модификатора. Attack по другой цели, Melee/Brawn Attack и любое не-ATTACK действие возвращают `LOST`. Граница чистая и не хранит скрытого состояния: будущий battle aggregate обязан вызвать её ровно один раз и удалить snapshot после результата. Ranged-weapon preparation теперь требует Aim и explicit GM approval для Extreme Range и использует тот же follow-up для подготовленной профильной атаки; окончательное владение consumed snapshot остаётся за orchestration.

Help executor связывает active actor, другого участника той же стороны, зарезервированный `CombatActionKind.HELP`, собственный `TestRequest` помогающего и stable ID будущего Test союзника. Skill обоих Tests хранится явно; несовпадение разрешается только с `different_skill_approved_by_gm=True`. Каждый success создаёт один обычный bonus die, а failure всё равно завершает action с нулевым `HelpBonusSnapshot`. `Deafened` прямо запрещает Help, а `Defenceless` запрещает действие до обращения к RNG.

`apply_help_bonus` принимает только совпадающие beneficiary ID, Skill и `TestRequest.id`, добавляет обычный `DiceModifier` под общим pool cap либо погашает нулевой snapshot без модификатора. В отличие от Aim, книга не требует потерять Help из-за промежуточного действия: snapshot связан с конкретной upcoming Test, включая потенциальную Defence Test. Future aggregate обязан однократно хранить и погасить его; профильные бонусы к самой Help Test передаются обычными модификаторами исходного `help_test`.

Recover executor использует закрытый `RecoverMode`. В `STANDARD` actor одновременно получает все выбранные доступные выгоды: снимает Staggered с self либо Close ally, снимает Prone с независимо выбранного self/Close ally либо вместо этого создаёт mount follow-up, уменьшает собственный Miscast Pool на один die до минимума 0 и опционально создаёт follow-up взаимодействия с одним объектом Close даже при наличии врага. Снятия двух Conditions с одной цели объединяются в один immutable transition; разные цели сохраняют стабильный порядок Staggered → Prone. Mount и object остаются типизированными запросами, пока отсутствуют mount/inventory states.

Альтернативные `TREAT_WOUND` и `REMOVE_CONDITION` не получают ни одной standard-выгоды. Treatment требует выбранную untreated Wound, подходящие trappings и либо Recall Test, либо auto-success от явно релевантного Lore; успех создаёт `RecoverWoundTreatmentApplicationRequest`. Отдельный `RecoverWoundTreatmentResolutionRequest` принимает точный успешный action result и неизменившийся injury snapshot, отмечает только выбранную Wound как treated и удаляет только её эффекты `UNTIL_TREATED`. Condition снимается лишь при явном source snapshot об отсутствии другого активного источника; известный эффект другой раны принудительно сохраняет её. Application ID однократно добавляется в ordered consumed snapshot, а второго action receipt не создаётся. Condition alternative выполняет один явно релевантный Test и при успехе снимает ровно одну не-Staggered/не-Prone Condition; external fact `underlying_cause_allows_removal` запрещает бросок, пока действует источник Condition. Failure обеих Test-ветвей всё равно завершает action. Defenceless закрывает Recover, а Broken может Recover только в Zone без врага.

`EndBattleWoundTreatmentRequest` реализует отдельное automatic Recover без action slot и Test. Внешний target-scoped context обязан подтвердить завершённый бой и возможность перевести дух; цель должна быть жива и иметь подходящие trappings для всех ран. Reducer отмечает treated все её ещё untreated Wounds, удаляет их `UNTIL_TREATED` effects по той же source-aware Condition policy и однократно погашает context ID. Уже treated Wounds, эффекты `UNTIL_HEALED`/`PERMANENT` и несвязанные Conditions сохраняются. Если подходящих trappings недостаточно для всех ран, текущая граница отклоняет запрос, не выбирая частичное лечение скрытой политикой (`AMBIGUITY-008`).

`CatchYourBreathHealingRequest` является отдельной end-encounter фазой и принимает `EndEncounterHealingOpportunity`, подтверждающий завершение боя либо опасного столкновения и окончание непосредственной угрозы. Opportunity может ссылаться на точный `EndBattleWoundTreatmentResult`; если нужные раны были обработаны раньше, такая ссылка не обязательна. Каждая выбранная Wound всё равно обязана быть treated и иметь resolved effect, поэтому источник окончания опасности не обходит treatment/trappings. Reducer сверяет каждую историческую запись с Wounds Table и за один переход полностью исцеляет все и только treated/resolved, ещё не healed Wounds категории `CATCH_YOUR_BREATH`; необработанная рана, включая другую лёгкую Wound, не блокирует готовые к healing записи и сама не исцеляется. Более долгие категории не затрагиваются. Вылеченная Wound остаётся в журнале с прежним sequence, но больше не считается активной; все её непостоянные effects заканчиваются независимо от их прежнего duration, а `PERMANENT` сохраняются. Общая Condition удаляется только при explicit отсутствии другого источника. Opportunity ID погашается один раз; эта фаза не создаёт action receipt, Test или RNG.

Источник: страница 121 и Wounds Table на страницах 190–191.

`NightsRespiteHealingOpportunity` представляет книжное `A Night’s Respite`: target провёл период спокойно, завершил ранний ночной отдых, и наступило утро. Контекст не задаёт вымышленное число часов и не исполняет optional досрочную Endurance-проверку. `NightsRespiteHealingRequest` использует тот же source-aware healing transition и исцеляет все и только treated/resolved, ещё не healed Wounds категории `NIGHTS_REST` — строки `8–15` Wounds Table. Необработанные записи и категории `CATCH_YOUR_BREATH`, `REST_AND_RECOVERY` и `SURGERY_AND_RECOVERY` не меняются. Все non-permanent effects выбранных Wounds заканчиваются; permanent, чужие effects и независимо sourced Conditions сохраняются. Rest-opportunity ID погашается один раз без action receipt, Test или RNG.

`RestAndRecoveryEndeavourRequest` представляет одну выбранную Endeavour в конкретном downtime и проводит книжную Endurance Test. Провал не создаёт healing source. Успех создаёт `RestAndRecoveryEndeavourResult`, разрешает отдельной application-фазе исцелить ровно одну выбранную treated/resolved Wound и одновременно возвращает typed `FesteringWoundsRecoveryRequest` для всех Festering Wounds. Строки `16–19` проходят напрямую; строки `20–23` требуют успешный ordinary `DowntimeSurgeryResult` для той же Wound/target/state/downtime. Healing reducer переиспользует общий transition, сохраняет permanent effects и однократно погашает surgery proof перед Endeavour ID. Независимый Festering consumer погашает follow-up ID и очищает отдельный target-bound state, не изменяя ordinary Wounds.

Ordinary surgery выполняет отдельную Dexterity Test и требует Anatomy Lore, operating theatre, specialist tools, time и recovery supports. Успех создаёт proof без изменения injury state. Провал не выбирает книжный риск скрытой policy, а возвращает GM-owned `SurgeryFailureRiskRequest` с permanent disfigurement/death. Первая ветвь Combat Surgeon после успешного treatment выполняет дополнительную Recall Test: success создаёт source-aware suppression всех `UNTIL_HEALED` effects этой Wound до конца указанного battle без healing или удаления provenance; failure ничего не подавляет, а treatment trigger в обоих случаях погашается. Вторая ветвь исполняет battle surgery как non-attack Ability Improvise: каждая action даёт одну Dexterity Test в Exacting progress `8`, failed contribution не уменьшает progress и возвращает тот же GM-owned risk, completion создаёт proof без немедленного healing. По `AMBIGUITY-010` theatre отменён, tools/supports пока обязательны. Paid NPC service и применение внешних outcomes не входят в эти boundaries.

Источник: страницы 74, 121–122, 136 и Wounds Table на страницах 190–191.

Первый специализированный executor подключает `CombatActionKind.ATTACK`. `AttackActionExecutionRequest` связывает active actor, конкретный зарезервированный slot, явно выбранный target ID и готовый `KernelAttackRequest`. После полного `resolve_kernel_attack` slot получает неизменяемый `ActionExecutionReceipt`, а `AttackActionExecutionResult` возвращает round state до/после и вложенный `ResolutionResult`. Не-ATTACK, Charge, чужой/несуществующий, уже исполненный slot либо slot перед незавершённым более ранним действием отклоняются до RNG. Если Attack является вторым action за Fate, предшествующий `FateSecondActionSpendRequest → Result` уже атомарно списал расход и зарезервировал этот slot; executor не тратит ресурс повторно. Выбор цели и перенос `target_state` в будущий общий battle state остаются внешними фазами.

Второй узкий executor подключает только `ImproviseKind.SPELL`. Stable approach ID такого slot обязан совпадать с объявленным Lore в `CastingTestRequest`; caster обязан быть active actor. `execute_casting_attempt` выполняет ровно один Casting Test и только после его успеха добавляет receipt, возвращая полный `CastingTestResult`. Следующая `resolve_casting_action_post_test` сначала применяет его Rule of Nine follow-up к Miscast Pool. При безопасном пороге она требует явный связанный `CastingDecisionRequest` и выполняет normal `CAST`/`WAIT`; при сработавшем Miscast normal decision запрещён. `prepare_casting_action_miscast` связывает triggered result с готовым `MiscastPreparationRequest`: без spell создаётся один roll, а выбранный допустимый spell строго предшествует roll и добавляет `+1d`. Target discovery, spell/table effect и сам Miscast roll не выполняются автоматически.

Третий executor подключает `ImproviseKind.SKILL`. `SkillImproviseActionExecutionRequest` связывает active actor/round, typed `SkillImproviseApproach` со stable ID и один готовый basic либо opposed Test. Победа basic Test означает хотя бы один success; для opposed Test требуется победа initiator, включая только явно заданный tie-break. Success и failure одинаково завершают slot и сохраняют полный Test trace. Defenceless, другой Improvise kind, несовпавший approach ID и объявленный `improvise_produces_attack` закрываются до RNG; атакующая импровизация требует отдельного kernel composite.

Первая узкая effect-ветвь принимает `SkillImproviseConditionEffect`, само наличие которого означает внешнее GM approval с обязательным stable `gm_approval_id`. Только успешный Test создаёт `SkillImproviseConditionApplicationRequest`; failure и успешная творческая проверка без поддержанного effect не создают универсального narrative follow-up. Прямо поддержаны лишь два книжных примера — `Prone` и `Distracted`; другие Conditions и эффекты требуют специализированных application/resolution boundaries. Текущий request сохраняет target, classification и provenance, но ещё не применяет Condition к общему battle state.

Четвёртый специализированный executor подключает базовую ветвь `ManoeuvreKind.RUN`. `RunActionExecutionRequest` связывает active actor/round, зарезервированный Run slot и отдельный `SpatialBattleState`. Slow, Burdened, Prone и Defenceless отклоняются; Normal/Fast перемещаются ровно в одну соседнюю Zone. Enemy path blocker, obstacle и Difficult Terrain также требуют внешней либо отдельной фазы. Сначала создаётся новый spatial snapshot, и лишь затем выбранный slot получает `ActionExecutionReceipt`; при ошибке оба исходных состояния остаются неизменными. Ход нельзя завершить, пока зарезервированный Aim, обычный Attack, Help, Recover, Run, Charge, Move Quietly, Move Carefully либо Skill/spell/Ability Improvise не исполнен.

Пятый executor подключает базовый Charge по цели на Medium Range. `ChargeActionExecutionRequest` связывает зарезервированный Charge slot, active actor/round, вражеский target, movement context и готовый `KernelAttackRequest`. Zone adjacency доказывает Medium Range; попадание в Close Range после входа в Zone цели остаётся отдельным локальным фактом. Slow/Burdened/Prone/Defenceless, враг Close в начале хода, obstacle и enemy path blocker закрывают действие до RNG. Прямой executor отклоняет Difficult Terrain, а terrain-aware composite принимает готовое пересечение. После spatial mutation либо принятия crossed state выполняется ровно одна Close Range attack, и только после её результата slot получает receipt.

Для `Skill.MELEE` executor добавляет к attacker Test один обычный `DiceModifier(+1d)` с Rule ID Charge. Другие Attack Skills бонус не получают; временная политика для неоднозначного Brawn описана в `docs/contradictions.md`. Исходная и подготовленная `KernelAttackRequest` сохраняются в результате, поэтому бонус нельзя добавить дважды или скрыть в исходном запросе. Ход нельзя завершить с неисполненным Charge.

Шестой executor подключает попытку Charge по цели ровно на Long Range. `LongChargeActionExecutionRequest` принимает тот же зарезервированный slot, готовые Athletics Test и Close attack, а также промежуточную Zone двухзвенного маршрута. Соседство `origin → intermediate → target` при отсутствии прямой связи доказывает Long Range. Все movement/path/Condition проверки и конфликт с Athletics Test для Difficult Terrain того же turn выполняются до RNG.

При успехе Athletics actor входит в Zone цели, достигает явно подтверждённого Close Range и выполняет attack с той же Melee-only политикой `+1d`. При провале actor всё равно перемещается в промежуточную Zone — ровно за одну Zone от цели, — attack не подготавливается и не выполняется, а Staggered добавляется только при его отсутствии. Обе ветви завершают исходный Charge slot одним receipt; result сохраняет Test trace, состояния до/после, Condition application и optional kernel result.

Если actor уже имеет активный Casting snapshot и вместо следующего Casting Test исполняет любое другое действие, `SkippedCastingTestAfterActionRequest` связывает его проверенный `ActionExecutionReceipt` с актуальным `WizardMagicState`. Receipt сохраняет actor, round, slot и точную декларацию действия; slot, turn и round snapshots проверяют эти поля при создании состояния. Reducer отклоняет spell `Improvise`, создаёт source-aware увеличение пула ровно на один die и немедленно применяет общий Miscast threshold, не меняя Lore или накопленные successes. Явная цепочка `consumed_action_execution_ids` позволяет последовательно обработать разные actions и запрещает повторно начислить die за один execution ID до появления общего battle aggregate.

## RULE-COMBAT-005 — выбор навыков атаки и защиты

- Melee используется с подходящим оружием ближнего боя.
- Shooting используется для стрелкового оружия.
- Throwing используется для метательного оружия.
- Brawn используется для безоружных атак.
- Осведомлённая цель обычно Oppose атаке через Athletics либо Defence, если снаряжение позволяет.
- Defence требует оружия или щита против Melee; против Shooting/Throwing Defence обычно требует щита.
- неосведомлённая или Defenceless цель не выполняет встречную защиту.

Источник: страницы 68, 92, 94, 117–118.

## RULE-COMBAT-006 — успех атаки

Атака успешна, когда атакующий получил не меньше успехов, чем защитник. Для unopposed атаки нужен хотя бы один успех, если специальное правило не говорит иначе.

Источник: страницы 109, 118.

## RULE-COMBAT-007 — промах

Промах атакой по врагу в Close Range накладывает на атакующего Staggered, только если этого состояния ещё нет. Повторный промах не усиливает уже имеющийся Staggered. Обычная дальняя атака не создаёт этот штраф; дальнее оружие при атаке в Close Range следует правилу ближнего промаха.

Источник: страницы 119 и 94.

## RULE-COMBAT-008 — урон

```text
Damage = base weapon Damage + attacker_successes - defender_successes
```

Для unopposed атаки вместо разницы добавляется общее число успехов атакующего. Базовый урон оружия может быть статическим или производным от Strength.

Источник: страница 119 и таблицы оружия на страницах 93, 95–96.

## RULE-COMBAT-009 — модификаторы атаки

На пул влияют Charge, численное преимущество в Zone, высота, дистанция, укрытие, положение Prone, свойства оружия, Help, Wounds, Talents и другие Abilities. Модификаторы должны вычисляться из контекста действия, а не сохраняться как постоянное эффективное значение характеристики.

Источник: страницы 115, 118–119 и таблицы снаряжения.

## Реализация Attack/Impact в K1

- контракты: `src/towr/domain/attack_models.py`;
- чистое разрешение одной атаки: `src/towr/rules/attack_resolution.py`;
- детерминированные проверки: `tests/unit/test_k1_attack_resolution.py`.

Чистый Attack resolver поддерживает одну основную цель, opposed/unopposed атаку, книжный tie-break, обычный Damage, коэффициент успехов профильной атаки, эффективный Resilience, игнорирование брони и последствие промаха в Close Range. `resolve_kernel_attack` применяет к основной цели Staggered, Wound и replacement impact, исполняет фазовый `ProneBeforeGiveGroundSpec` и возвращает остальные действия типизированными follow-up. Поиск вторичных целей по Zones остаётся вне K1.

`execute_attack_action` является orchestration-адаптером над этим kernel и не дублирует правила атаки. Он обновляет только execution receipt выбранного action slot; состояние цели остаётся в типизированном `ResolutionResult` до появления общего battle aggregate.

## RULE-COMBAT-010 — завершение боя

Книга предусматривает поражение, недееспособность, сдачу и групповой Retreat. Для симулятора ещё требуется формальная политика определения исхода столкновения.

Источник: страницы 117, 120 и 191. Статус: `needs_clarification` для целей симуляции.

## RULE-COMBAT-011 — Zones и Ranges

Zone имеет контекстный размер/форму и следует естественным границам сцены. Position внутри Zone сохраняет значение для line of sight, cover, concealment, окружения и препятствий.

Ranges: Close — arm’s reach; Short — та же Zone; Medium — одна Zone; Long — две; Extreme — три и более. Эти определения являются graph/spatial input, а не фиксированными метрами.

Источник: страница 114.

## RULE-COMBAT-012 — Speed и ограничения движения

- Slow: free move до Medium, без Run/Charge/Move Quietly/Move Carefully;
- Normal: free move до Medium и доступ к Manoeuvre;
- Fast: free move до Long и доступ к Manoeuvre.

Burdened запрещает Manoeuvre, Prone — выход из Zone, Defenceless — любое движение. При длительной погоне более высокий Speed в итоге догоняет; при равенстве либо head start используется Opposed Athletics.

Источник: страница 115.

## RULE-COMBAT-013 — battlefield features

Cover/concealment даёт атакующему `-1d` Shooting. В темноте Long/Extreme не видны; attack по невидимой цели считается Blinded и Grim. Difficult Terrain требует Athletics при пересечении, провал роняет Prone после пересечения; Move Carefully и конкретный Lore обходят Test. В тот же turn нельзя также Athletics для дополнительной Zone Run/Charge.

Источник: страница 115.

## RULE-COMBAT-014 — free actions и Manoeuvre

Раз за turn даётся free move по Speed; вместо него без врага Close можно снять Prone с себя/союзника Close. К incidental actions относятся draw/swap, non-Test reload prep, добровольный Prone, короткая речь и простое взаимодействие Close; рядом с врагом взаимодействие требует Recover.

Run добавляет одну Zone и может добавить вторую успешной Athletics, провал впервые даёт Staggered. Charge достигает цели Medium, даёт Melee `+1d`; Long требует Athletics, провал останавливает за Zone, отменяет attack и впервые даёт Staggered. Charge запрещён при начале Close к врагу. Move Quietly — Stealth против наиболее vigilant Awareness и требует cover/concealment. Move Carefully игнорирует Difficult Terrain и может добавить Awareness search.

Источник: страницы 116–117.

### Реализация free movement в K1

`MovementSpeed` типизирует Slow, Normal и Fast. `FreeMovementRequest` связывает `SpatialBattleState` с активным actor из отдельного `CombatRoundState`, требует одинаковый номер round и принимает явный маршрут по Zone graph. Slow/Normal могут пересечь одну границу Zone (Medium Range), Fast — до двух (Long Range). Это incidental-фаза: action slot не резервируется и round state не мутируется.

После успешного перехода меняется только Zone actor и `free_move_used_entity_ids`. Список хранится до следующего spatial round: при текущей модели один actor получает один ход за round, поэтому он однозначно представляет книжный предел once per turn отдельно от `gave_ground_entity_ids`. Prone и Defenceless блокируют движение, Burdened не блокирует free move. Явно переданный enemy path blocker или obstacle отклоняет маршрут; союзник на пути не считается врагом.

Difficult Terrain имеет отдельную общую Athletics movement-фазу. Free move, базовый Run и Medium Charge принимают её готовый результат через отдельные composite adapters; прямые reducers по-прежнему закрывают terrain route. Текущий terrain-aware free move покрывает одну границу Zone: Fast-маршрут с несколькими terrain/non-terrain сегментами требует будущего route orchestration. Перемещение позиции внутри одной Zone не представимо одним `zone_id` и остаётся внешним spatial context.

`FreeMoveProneRemovalRequest` представляет книжную альтернативу тому же free move. `ProneRemovalTargetKind` явно различает `SELF` и `ALLY`; для союзника обязателен внешний Close Range fact, совпадение `side_id` и отличный от actor target ID. Отдельный `actor_has_enemy_in_close_range` нельзя выводить из общей Zone, поскольку Short и Close Range не совпадают. При наличии такого врага, отсутствии Prone или уже использованном free move reducer закрывается без изменений. Успех удаляет только Prone из переданного `ConditionState`, не меняет placements/action slots и записывает actor в общий `free_move_used_entity_ids`, поэтому после снятия Prone обычное движение уже недоступно и наоборот.

### Реализация базового Run в K1

`execute_run_action` требует уже зарезервированный Run slot и завершённость более раннего slot. Базовая дистанция не зависит от Normal/Fast и равна одной дополнительной соседней Zone; она не расходует и не восстанавливает отдельный free-move usage. Round и spatial snapshots имеют общий номер, а successful result меняет только placement actor и receipt выбранного slot.

Опциональный Athletics Test для второй дополнительной Zone выполняется отдельным `RunAthleticsExtensionRequest → RunAthleticsExtensionResult` после базового Run. Само наличие запроса означает добровольный выбор попытки; он принимает готовый `TestRequest`, вторую соседнюю destination и явный path context. Все spatial-проверки происходят до RNG. Успех перемещает actor ещё на одну Zone; провал оставляет placement прежним и добавляет Staggered только при отсутствии этого Condition. Уже имеющийся Staggered не превращается в repeated-Staggered choice.

Фаза не создаёт второй receipt и сохраняет free-move/Give Ground usage. Наличие actor в `SpatialBattleState.difficult_terrain_tested_entity_ids` запрещает попытку после Athletics Test для Difficult Terrain; вызывающая сторона больше не передаёт доверенный boolean. Сама extra Zone также не может одновременно пересекать Difficult Terrain. Регистрация уже выбранной optional-фазы и защита от повторного воспроизведения одного base result остаются ответственностью будущего battle aggregate.

Terrain-aware базовый Run разделён на фактическое пересечение и bookkeeping action slot. `DifficultTerrainRunActionExecutionRequest` требует точного совпадения исходных round/spatial snapshots, actor, Conditions, destination, path и obstacle с сохранённым source traversal, а current snapshots — с ещё неисполненным Run slot и crossed state. Затем adapter только добавляет receipt, не бросает Athletics повторно и не меняет spatial state. Провал terrain Athletics поэтому завершает Run уже в destination Zone и сохраняет Prone.

### Реализация Charge в K1

Medium и Long используют один Charge slot и одинаковую подготовку последующей атаки, но разные типизированные composite contracts. Medium требует одну границу Zone и всегда доходит до attack после preflight. Long требует ровно две границы и сначала выполняет Athletics: success доходит до цели и атакует, failure останавливается в промежуточной Zone, впервые применяет Staggered и завершает действие без attack. Ни одна ветвь не расходует и не восстанавливает free-move/Give Ground usage.

### Реализация Difficult Terrain в K1

`DifficultTerrainTraversalRequest → DifficultTerrainTraversalResult` представляет одно фактическое пересечение границы Zone через terrain. Request связывает active actor/round, исходный spatial snapshot, готовый Athletics `TestRequest`, Conditions, destination и локальный path context. Result сохраняет полный source request и дублирует проверяемые path/obstacle facts, чтобы следующий adapter мог доказать provenance. Prone/Defenceless, obstacle, enemy blocker и несоседняя Zone закрываются до RNG; Burdened не блокирует общий traversal, поскольку Difficult Terrain встречается и при free move.

Reducer сначала создаёт spatial snapshot после пересечения и записывает actor в `difficult_terrain_tested_entity_ids`, затем разрешает Athletics. Success сохраняет Conditions, failure немедленно добавляет Prone через общий Condition reducer, не отменяя movement. Повторное пересечение в том же turn требует новый Test, но не дублирует usage ID. `start_next_spatial_round` очищает usage вместе с Give Ground/free move. Optional Run и Long Charge читают этот авторитетный факт и закрываются до RNG.

`DifficultTerrainFreeMovementRequest` принимает только исходный free-move request с terrain flag, точно соответствующий сохранённому traversal source, и current spatial state, равный crossed state. Adapter добавляет лишь `free_move_used_entity_ids`; failed Athletics сохраняет движение и Prone. Аналогичный Run composite принимает ещё current round state и добавляет только action receipt. Подмена result или его применение к уже обновлённым snapshots отклоняется.

`DifficultTerrainChargeActionExecutionRequest` связывает тот же traversal с исходным Medium Charge. Он требует точного совпадения actor/round/origin/target destination/path/Conditions и current crossed state, после чего выполняет Close attack и добавляет Charge receipt, не двигая actor повторно. Failed terrain Test сохраняет Prone, но не отменяет атаку: по странице 123 Prone запрещает последующий выход из Zone и модифицирует атаки против лежащего, а не его собственную атаку. Post-terrain Staggered fact всё равно обязан совпасть с `AttackRequest`; Melee получает обычный `+1d`, другие Skills следуют временной политике `AMBIGUITY-007`.

### Реализация Move Carefully в K1

`MoveCarefullyActionExecutionRequest` связывает active actor/round, зарезервированный `ManoeuvreKind.MOVE_CAREFULLY` и обычный `FreeMovementRequest`, чей маршрут явно помечен как пересекающий Difficult Terrain. Это одно действие: reducer проверяет movement context, переносит actor по тому же Zone graph, расходует общий `free_move_used_entity_ids` и лишь затем добавляет receipt. Athletics Test не создаётся, а `difficult_terrain_tested_entity_ids` и прочие spatial usage сохраняются без изменений.

Normal проходит одну Zone boundary, Fast — до двух последовательных связей. Slow, Burdened, Prone, Defenceless, obstacle, enemy path blocker, неизвестный/несвязный маршрут, уже использованный free move и нарушение порядка slots закрываются до RNG. Союзник на пути не блокирует движение. Зарезервированный Move Carefully нельзя оставить неисполненным и завершить ход.

Необязательный Awareness search выражен закрытым выбором `MoveCarefullySearchChoice.DECLINE` либо `SEARCH`. `DECLINE` не принимает Test и не обращается к RNG. `SEARCH` требует `Skill.AWARENESS`, выполняет общий `TestRequest → TestResult` после movement preflight и сохраняет полный trace; интерпретация найденной скрытой возможности, слабости или противника принадлежит будущему scene/battle orchestration. Обе ветви завершают тот же slot и не создают дополнительного перемещения.

### Реализация Move Quietly в K1

`MoveQuietlyActionExecutionRequest` связывает active actor/round, зарезервированный `ManoeuvreKind.MOVE_QUIETLY`, Stealth/Awareness `OpposedTestRequest` и стабильный снимок eligible `MoveQuietlyObserver`. Книга не задаёт числовую формулу «most vigilant», поэтому orchestration передаёт каждому наблюдателю явный `vigilance_priority`; executor выбирает максимальный, а равенство разрешает порядком снимка. Бросается Awareness только выбранного наблюдателя. Ненулевая ничья принадлежит инициатору как стороне, меняющей положение; `0:0` остаётся провалом обеих сторон.

Cover/concealment и конкретный hiding spot являются явными spatial facts. `MoveQuietlyHidingChoice` различает `DECLINE`, укрытие внутри текущей Zone и укрытие по Zone-route. Последняя ветвь предварительно проверяет условный `FreeMovementRequest` до opposed RNG; same-Zone ветвь меняет только free-move usage. При победе обе hiding-ветви расходуют обычный free move и создают `MoveQuietlyHiddenAttackOpportunity`: следующая attack из этого `hiding_position_id` должна быть unopposed. `MoveQuietlyHiddenAttackExecutionRequest` проверяет исходный hidden result, текущие placement/hiding-position, всё ещё unaware выбранную цель и готовую Attack с `defender_test=None`, затем атомарно вызывает обычный Attack executor, погашает opportunity ID без второго receipt и возвращает раскрытый `hiding_position_id` для регистрации в `HidingPositionState`. Loss consumer погашает возможность при уходе с позиции или неподходящем Attack (иная/aware цель); другое действие само по себе не является причиной потери. Opportunity consumed-ID snapshot передаёт caller; actor-scoped `used_hiding_position_ids` теперь хранится в `HidingPositionState` и обновляется отдельной регистрацией завершённой hidden Attack (см. ниже).

`MoveQuietlyHiddenAttackContinuationRequest → Result` обрабатывает завершённое non-attacking действие по `ActionExecutionReceipt`, актуальному spatial/hiding-position snapshot и обязательному bool `position_revealed`. Неизменная позиция без раскрытия возвращает `PRESERVED`, исходную opportunity и ту же consumption chain. Уход с позиции либо явное раскрытие возвращают `LOST` с `LEFT_HIDING_POSITION`/`POSITION_REVEALED` и один раз дописывают opportunity ID; при одновременных причинах приоритет у ухода. Проверяются actor, round, порядок после Move Quietly, источник opportunity и отсутствие её ID в consumed snapshot. Attack, Charge и attacking Improvise не допускаются в эту границу. Она не бросает кубы, не создаёт receipt, не меняет spatial state и не расходует Aim. Сохраняющий переход идемпотентен при том же актуальном snapshot; caller обязан передавать обновлённую chain после потери/атаки. Раскрытие не выводится автоматически из вида действия: это внешний факт для данной opportunity. `OTHER_ACTION` удалён, stationary non-Attack запрос в прежний loss consumer отклоняется с направлением к continuation. Основание: PG 1.4, Rules / Manoeuvre и Attack Tests, стр. 117–118; `CODE-CONFLICT-001` исправлен.

`PreparedHiddenRangedAttackExecutionRequest → Result` связывает существующий hidden preflight с `PreparedRangedWeaponAttackExecutionRequest` по точному final Attack, уже включающему профиль и optional Aim. Исполняется только `execute_prepared_ranged_weapon_attack`, ровно один раз; после полного результата погашается hidden opportunity и возвращается revealed hiding-position ID. Result хранит один nested prepared result, exact source и объединённый ordered trace подготовки/Move Quietly/execution. Hidden consumption chain хранится отдельно, Aim chain доступна из вложенного результата без копирования; hit и miss расходуют обе применимые возможности, а исключение не мутирует входные snapshots (RNG не откатывается). Текущие awareness/placement и обе consumption chains сообщает caller. Это consumer уже подтверждённой возможности, не dispatcher lifecycle скрытности.

Провал opposed Test завершает slot без spatial mutation и расхода free move. Победа без подготовленного cover/movement также завершает slot как `SUCCEEDED_WITHOUT_HIDING`, но не делает врагов unaware. Slow/Burdened/Prone/Defenceless, enemy/obstacle/Difficult Terrain path, уже использованный free move, stale snapshots и нарушение slot order закрываются до бросков или мутации.

Как и остальные pure reducers, точный повтор одного неизменного request воспроизводит тот же результат; глобальную дедупликацию request ID должен обеспечить будущий battle aggregate.

## RULE-COMBAT-015 — Give Ground и Prone

Give Ground перемещает от attacker в выбранную adjacent Zone, максимум раз/round. Вход во вражескую Zone даёт Broken. Путь не проходит через enemies, obstacles или Difficult Terrain; Prone/невозможность покинуть Zone запрещают выбор.

Prone нельзя получить повторно или сочетать с Give Ground. Если уже Staggered+Prone снова получает Staggered, результатом становится Wound.

Источник: страницы 119–120.

### Реализация spatial Give Ground в K1

`ZoneGraph` хранит стабильные Zone ID и неориентированное соседство без метров и координат. `SpatialBattleState` отдельно хранит стабильный список размещений и round-scoped факты Give Ground, free move и хотя бы одного Difficult Terrain Test. Стороны представлены coalition-like `side_id`: сущности с разными значениями считаются врагами.

`GiveGroundResolutionRequest` принимает уже выбранную destination Zone, Conditions движущегося существа и явный локальный снимок пути. Общий reducer проверяет соседство, увеличение graph-distance от атакующего при наличии `away_from_entity_id`, Prone/Defenceless, round limit, enemy blockers, obstacle и Difficult Terrain. После успешной проверки он меняет только размещение и round-scoped usage. `GiveGroundResolutionResult` хранит состояния до/после и проверяет точность этой мутации. Вход в Zone с врагом затем накладывает Broken через общий Condition reducer.

Книга не позволяет вывести путь внутри Zone только из графа. Поэтому `path_entity_ids`, `crosses_obstacle` и `crosses_difficult_terrain` сообщает orchestration/GM policy; это не скрытые defaults. Move Quietly уже принимает готовые cover/concealment и hiding-position facts, но их discovery, выбор destination, vertical/midair preference и общий line of sight остаются снаружи spatial executor. Решение границы зафиксировано в [`ADR-0003`](../decisions/ADR-0003-zone-graph-and-spatial-boundary.md).

## RULE-COMBAT-016 — Retreat

Только единогласная группа объявляет Retreat в начале round/своего side turn. Один персонаж тратит Fate на rearguard; без Fate GM назначает blood, materiel или misfortune cost. При pursuit каждый PC делает Athletics, иногда auto-success от Lore и/или opposed более быстрым врагом. За каждого провалившего бросается `1d10`, суммы определяют Run For Your Lives result; Complications могут вызвать roll даже без провалов.

Источник: страница 120. Табличные исходы `1–3` Lost, `4–6` Mocked, `7–9` Indebted, `10–12` Marked, `13–15` Exposed, `16–18` Hunted, `19–21` Robbed, `22–24` Surrounded, `25+` Trapped требуют campaign-state orchestration.

В K1 `GroupRetreatDeclaration` фиксирует battle, ordered состав игровых персонажей, инициатора, отдельный полный список согласившихся и актуальный `CombatRoundState`. Если игроки действуют первыми, объявление допустимо до первого хода round; если первым действует противник — только в начале стороны игроков после полностью завершившей стороны противника. Союзные NPC не становятся PC автоматически: книга отдельно оставляет судьбу сопровождаемых NPC и добычи ведущему.

`FateTacticalRetreatSpendRequest → Result` атомарно расходует Fate одного участника и создаёт связанный с session/actor/battle/group proof; только такой proof создаёт `RetreatRearGuardResult`. Этот результат подтверждает rearguard и полный покрытый состав, но намеренно сохраняет `pursuit_decision_required=True`: он не объявляет отсутствие погони и не исполняет Athletics/table consequences.

Если точные Fate states всей PC-группы исчерпаны, `prepare_retreat_alternative_price` возвращает GM-owned запрос с тремя книжными классами цены. `resolve_retreat_alternative_price` требует отдельный stable decision и выбирает ровно один вариант: `blood`, `materiel` либо `misfortune`. Result создаёт battle/Retreat/group-bound proof и один соответствующий follow-up: Wound для ещё не выбранного PC, один valuable trapping ещё не выбранного владельца либо одну golden opportunity для opposition snapshot. Ни цель, ни конкретная Wound, ни предмет, ни содержание возможности не выбираются скрыто.

Blood follow-up исполняется отдельной двухфазной границей. `RetreatBloodPriceWoundRequest` принимает полный canonical price-result, уже выбранный внешний policy `target_id`, актуальный `CharacterInjuryState` и обычные Wound modifiers/negation options. Target обязан входить в исходный PC snapshot; reducer не выбирает его и не принимает materiel/misfortune proof. `begin_retreat_blood_price_application` погашает application ID один раз, бросает ровно одну обычную Wound через общий character lifecycle и останавливается перед effect. Exact completion затем либо регистрирует Wound за день и применяет строку таблицы, либо разрешает общий Near Miss. Near Miss отменяет саму Wound, но не возвращает уже погашенный alternative-price application: permanent Fate burn является отдельным фактически уплаченным ресурсом.

Materiel follow-up также не выбирает владельца или предмет. `RetreatMaterielPriceInventoryRequest` принимает canonical materiel result, explicit PC owner, stable ID конкретного carried trapping и полный минимальный `CarriedInventoryState` этого владельца. Выбранный `TrappingSnapshot` обязан присутствовать в snapshot и иметь внешнюю GM-классификацию `is_valuable=True`. Чистый `apply_retreat_materiel_price` удаляет ровно эту запись, сохраняет dropped trapping в результате и однократно погашает application ID. Он не назначает spatial position предмета, не вычисляет ценность из Cost tier и не принимает blood/misfortune proof.

Misfortune follow-up регистрирует кампанийный факт, но не исполняет его повествовательное содержание. `RetreatMisfortunePriceCampaignRequest` принимает canonical misfortune result, актуальный `CampaignGoldenOpportunityState`, одного явно выбранного врага из исходного opposition snapshot, stable ID возможности и stable reference на подготовленное GM описание. `apply_retreat_misfortune_price` добавляет ровно один `CampaignGoldenOpportunity`, сохраняет battle/Retreat/application provenance и делает source application одноразовым внутри campaign aggregate. Reducer не использует RNG, не выбирает beneficiary/описание и не создаёт encounter, Wound, потерю предмета либо иной конкретный эффект.

`RetreatPursuitResolutionRequest → Result` принимает один из двух закрытых cover-results — Fate-funded rearguard либо подтверждённую alternative price — и явный ordered список преследующих enemies. Пустой список означает решение не преследовать и не расходует RNG; непустой требует ровно одну попытку для каждого PC в исходном порядке группы. Поддержаны обычная Athletics Test, GM-approved Lore automatic success и Opposed Athletics Test против конкретного выбранного pursuer. Контекстуальный tie-break остаётся явной частью `OpposedTestRequest`, поскольку книга допускает несколько общих оснований для ничьей.

Marginal Success не получает скрытого последствия: отдельный provider обязан выбрать продолжение без Complication, принятую Complication со stable ID либо книжный отказ от слишком высокой цены с превращением успеха в failure. Result сохраняет ordered Test results, failed actor IDs, принятые Complication IDs, один обязательный будущий `1d10` за каждый failure и опциональное право GM на один table roll, только если все успели уйти и Complications получили как минимум двое.

`RunForYourLivesResolutionRequest → Result` бросает `1d10` для каждого failed PC строго в порядке исходной группы. При доступном варианте из нескольких Complications запрос нельзя создать без отдельного решения GM `ROLL`/`DO_NOT_ROLL`; согласие добавляет ровно один общий бросок, отказ не расходует RNG. Все броски сохраняют причину и provenance, затем суммируются и классифицируются в один из девяти книжных диапазонов от `Lost` до `Trapped`. Если бросков нет, сумма равна нулю и табличного результата нет. Положительный результат создаёт `RunForYourLivesCampaignConsequenceRequest` с battle/Retreat/group/cover-proof/failure/Complication context, но не теряет предметы и репутацию, не создаёт врагов, долг, пленение или раны: это остаётся отдельным GM-owned campaign application.

Первый campaign application слой также не исполняет разнородные последствия таблицы. `RunForYourLivesConsequenceSpecification` требует exact typed outcome, stable reference на GM-authored описание, непустые explicit references затронутых субъектов и уже выбранных конкретных consequences. `RunForYourLivesCampaignApplicationRequest` связывает specification с исходным follow-up и актуальным `CampaignConsequenceState`; mismatch любого из девяти outcomes отклоняется. `register_run_for_your_lives_campaign_consequence` без RNG добавляет один полный provenance-bearing `CampaignConsequenceRecord` и делает source follow-up одноразовым. Изменение времени, репутации, долга, enemy readiness, местоположения, inventory, injury либо captivity остаётся специализированным consumer соответствующего state.

Первый специализированный consumer реализован для `Robbed`. `RunForYourLivesRobbedInventoryRequest` принимает exact зарегистрированный outcome, ordered `CarriedInventoryState` каждого PC исходной группы и ordered `RobbedTrappingLossSelection` с непустым explicit набором carried item IDs для каждого владельца. Одна consequence reference на персонажа обязана точно совпадать с зарегистрированной specification; reducer не выбирает предметы и не вычисляет их вес. `apply_run_for_your_lives_robbed` без RNG атомарно создаёт переход inventory каждого PC, сохраняет dropped trappings и один раз погашает campaign consequence. Пустой inventory либо отсутствие выбранной потери у любого PC отклоняется до ruling `AMBIGUITY-013`; spatial placement и capacity/equipment recalculation не выполняются.

Для `Trapped` реализована отдельная граница выбора цены. `RunForYourLivesTrappedCostRequest` принимает только exact зарегистрированный outcome `TRAPPED`; GM-owned `TrappedEscapeCostDecision` явно задаёт ветвь `WOUNDS`, `CAPTURE` либо `OTHER`, непустой ordered набор затронутых PC и точные consequence references исходной specification. Resolver без RNG создаёт provenance-bearing `TrappedEscapeCostProof` и ровно один typed follow-up для выбранной ветви, после чего однократно погашает зарегистрированное consequence. Он не наносит Wounds и не меняет captivity/campaign state: каждую ветвь применяет отдельный consumer. Цель не выводится из rearguard; при alternative-price cover rearguard отсутствует, а затронутые PC всегда передаются явно. Число Wounds и точная семантика цены остаются explicit GM input по `AMBIGUITY-014`.

Ветвь `Trapped Wounds` исполняется отдельной последовательной application. `RunForYourLivesTrappedWoundRequest` связывает exact Wound follow-up с полным ordered набором `TrappedWoundCostTarget`: для каждого затронутого PC GM явно задаёт положительное число Wounds, актуальные injury/day snapshots, modifiers и доступные non-Fate negation options. Begin-фаза погашает application и бросает только первую Wound. Каждая advance-фаза принимает exact `CharacterWoundLifecycleCompletionResult`, сохраняет Near Miss, daily registration, Wound effect и consumed-ID chains, после чего бросает следующую Wound тому же PC либо переходит к следующему PC в исходном group order. Так принятая Wound влияет на untreated dice следующей, а отменённая — нет. Если completion убивает PC, неразыгранный остаток его назначения фиксируется как `skipped_wound_count`, но очередь остальных PC продолжается.

Ветвь `Trapped Capture` применяет только campaign-факт активного плена. `RunForYourLivesTrappedCaptureRequest` принимает exact capture follow-up, актуальный `CampaignCaptivityState` и ordered `TrappedCaptureAssignment` для каждого affected PC. Assignment явно задаёт unique capture ID, captor reference и одну exact consequence reference; один captor может удерживать нескольких PC. Reducer без RNG атомарно дописывает provenance-bearing `CampaignCaptivityRecord`, сохраняющий campaign/battle/Retreat/cost-proof/consequence/application trace, и делает source application одноразовым внутри aggregate. Уже активный captive, другой campaign/branch/order/reference, повтор capture ID/application и forged result отклоняются. Captor не выводится из rearguard либо opposition, а escape, release, rescue, inventory и injury effects не исполняются по `AMBIGUITY-015`.

Ветвь `Trapped Other` регистрирует только непрозрачную GM-defined цену побега. `RunForYourLivesTrappedOtherRequest` принимает canonical other follow-up, актуальный `CampaignTrappedOtherState` и unique cost ID. Reducer без RNG переносит exact ordered affected PC, consequence references, description reference и полную decision/proof/application/campaign/battle/Retreat provenance в один immutable `CampaignTrappedOtherCost`, после чего source application становится одноразовым. Aggregate не классифицирует содержание цены и не меняет injury, captivity, inventory, economy, relationship либо иной специализированный state; реальное применение конкретной цены требует отдельного consumer по `AMBIGUITY-014`.

Outcome `Surrounded` применяет только campaign-hook возможного нового конфликта. `RunForYourLivesSurroundedRequest` принимает exact зарегистрированный `SURROUNDED`, отдельный `CampaignConflictOpportunityState`, unique opportunity ID и две ordered role references: новую opposition и подготовку encounter. Они обязаны в точности совпасть с concrete consequence references исходной specification. Reducer без RNG однократно дописывает `CampaignConflictOpportunity` с affected subjects и полным application/consequence/specification/battle/Retreat provenance. Он не ищет врага в прежнем opposition snapshot, не запускает бой или переговоры, не выбирает Tests и не размещает участников; эти границы остаются внешними по `AMBIGUITY-016`.

Outcome `Hunted` использует отдельный `CampaignHuntState`. Registration request принимает exact зарегистрированный `HUNTED`, unique threat ID и distinct ordered role references `(pursuer, activation trigger)`, полностью совпадающие с concrete consequence specification; результат без RNG добавляет неактивный `CampaignHuntThreat` с affected subjects и полным provenance. Отдельный activation request принимает зарегистрированную неактивную угрозу, exact trigger reference и stable movement-event reference, после чего однократно добавляет `CampaignHuntActivation`. Один movement event может активировать несколько разных угроз. Классификация события как «move again», длительность lay-low, обнаружение, маршрут, pursuit и снятие/разрешение угрозы не вычисляются по `AMBIGUITY-017`.

Outcome `Indebted` регистрирует только незакрытое обязательство после спасения. `RunForYourLivesIndebtedRequest` принимает exact `INDEBTED`, актуальный `CampaignDebtState`, unique obligation ID и distinct ordered role references `(creditor, debt, repayment)`, полностью совпадающие с concrete consequence specification. Reducer без RNG добавляет `CampaignDebtObligation` с affected subjects и полным provenance, но не выбирает сумму, услугу, срок, санкцию или способ погашения; будущая выплата/исполнение требует отдельного transition по `AMBIGUITY-020`.

Outcome `Mocked` регистрирует только репутационное последствие увиденного провала. `RunForYourLivesMockedRequest` принимает exact `MOCKED`, актуальный `CampaignReputationState`, unique reputation-consequence ID, непустой ordered набор witness/rival references и distinct references на gossip incident и reputation effect; весь порядок обязан совпадать с concrete consequence specification. Reducer без RNG добавляет `CampaignReputationConsequence` с affected subjects и полным provenance, но не назначает числовой штраф, social scope, длительность или восстановление по `AMBIGUITY-021`.

Outcome `Lost` регистрирует только campaign delay после погони в незнакомую местность. `RunForYourLivesLostRequest` принимает exact `LOST`, актуальный `CampaignDelayState`, unique delay-consequence ID и distinct ordered role references `(unfamiliar territory, intended destination, return delay, enemy opportunity)`, полностью совпадающие с concrete consequence specification. Reducer без RNG добавляет `CampaignDelayConsequence` с affected subjects и полным provenance, но не выбирает длительность, маршрут, spatial movement, calendar advance или действия врага по `AMBIGUITY-022`.

Outcome `Marked` использует отдельный `CampaignEnemyReadinessState`. Registration request принимает exact зарегистрированный `MARKED`, unique readiness ID и distinct ordered role references `(enemy, acquired intelligence, next-action trigger)`, полностью совпадающие с concrete consequence specification; результат без RNG добавляет pending `CampaignEnemyReadiness` с affected subjects и полным provenance. Отдельный activation request требует ту же enemy/trigger pair и stable action-event reference, после чего однократно добавляет `CampaignEnemyReadinessActivation`; одно событие может активировать несколько разных readiness records. Aggregate лишь фиксирует наступление книжного «next time»: он не классифицирует действие, не назначает bonus, ambush или encounter и не исполняет preparedness по `AMBIGUITY-019`.

Outcome `Exposed` регистрирует только раскрытую врагу campaign-информацию. `RunForYourLivesExposedRequest` принимает exact `EXPOSED`, актуальный `CampaignIntelligenceState`, unique exposure ID и ordered role references `(enemy, home, shelters…, weakness)`, полностью совпадающие с concrete consequence specification. Shelters должны быть непустыми, а все role references — различными. Reducer без RNG добавляет `CampaignIntelligenceExposure` с affected subjects и полным provenance, но не выбирает атаку, жертву среди shelters, encounter timing или механический modifier; использование сведений остаётся отдельным consumer по `AMBIGUITY-018`.

## Регистрация раскрытого укрытия

`HidingPositionState` хранит actor-scoped ordered `used_hiding_position_ids` и `consumed_attack_execution_ids`. `HidingPositionRegistrationRequest → Result` принимает только завершённый ordinary/profile-aware/prepared hidden Attack. `register_revealed_hiding_position` без RNG и нового receipt добавляет раскрытую позицию и ID внутреннего Attack execution; hit и miss равноправны. Проверяются actor, точное совпадение истории с исходной Move Quietly, отсутствие позиции и execution ID в текущей истории. Новый ID регистрации не обходит replay-защиту. Result сохраняет exact source, before/after state и полный ordered trace; hidden/Aim consumption и weapon transition не изменяются.

`prepare_move_quietly_with_hiding_positions(state, request)` проверяет владельца и возвращает новый Move Quietly request с актуальной историей state; повторная валидация запрещает использованную позицию для same-Zone и route hiding до RNG. Candidate request служит шаблоном, его история заменяется явно переданным текущим state. Decline также сохраняет историю. Caller обязан сохранять новый state и использовать стабильные Attack/position IDs; чистые функции не обнаруживают повтор с намеренно старым snapshot и не обеспечивают конкурентную запись. История живёт в рамках переданного боевого контекста, автоматического сброса/переноса между боями нет. Awareness, cover, identity позиции и observer snapshot остаются внешними; общий battle aggregate не добавлен.

Источник: `BOOK-PLAYER-GUIDE` 1.4, Rules / Manoeuvre, стр. 117; Rules / Attack Tests, стр. 118. Rule ID: `RULE-COMBAT-014:revealed-hiding-position`. Код: `hiding_position_models.py`, `hiding_position_resolution.py`; тесты: `test_k1_hiding_position_resolution.py`.

`RegisteredHiddenAttackExecutionRequest → Result` и `execute_registered_hidden_attack` объединяют исполнение и регистрацию в один возвращаемый результат. Закрытый union принимает обычный, profile-aware либо prepared hidden request. Общий с standalone registration preflight проверяет actor, canonical Attack execution ID, неиспользованную позицию и точную историю исходной Move Quietly ещё при создании запроса, до RNG. Выбранный existing executor вызывается ровно один раз, затем его completed result передаётся registration consumer. Result хранит один registration result; `execution`, `previous_state` и `state` доступны как views без копий. Валидируются exact source request, registration ID, исходный state и ordered trace. Rule ID composite: `RULE-COMBAT-014:registered-hidden-attack`.

Атомарность относится к возвращаемым immutable snapshots: при исключении нет частичного результата, входные turn/weapon/Aim/hidden/history states не меняются. RNG и внешние эффекты decision provider не откатываются. Актуальный snapshot по-прежнему хранит caller; повтор с намеренно старым snapshot не обнаруживается. Standalone registration остаётся доступна для уже завершённых executions. Тесты composite: `test_k1_registered_hidden_attack_resolution.py` (10 детерминированных тестов).

Общий `MoveQuietlyHiddenAttackExecutionRequest` проверяет хронологию: `(attack.state.round_number, attack.slot_index)` должен быть строго позже `(move_quietly.round_state.round_number, move_quietly.slot.index)`. Более ранний round запрещён независимо от slot, в том же round запрещены тот же и более ранний slot. Более поздний round разрешает slot 1 даже после Move Quietly в slot 2; автоматического истечения opportunity между rounds нет. Проверка выполняется при создании hidden request, до RNG и decision provider, поэтому действует также для profile-aware/prepared и registered wrappers. Текущее placement/awareness и consumed snapshot по-прежнему сообщает caller. Основание: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117 («следующая атака»), и Attack Tests, стр. 118. Rule ID остаётся `RULE-COMBAT-014:hidden-attack-opportunity-consumption`; тесты: `test_k1_hidden_attack_chronology.py` (12 тестов с матрицей трёх ветвей × direct/registered).

Для атаки в том же round общий hidden preflight дополнительно требует active turn того же actor и точного равенства source slot из attack snapshot завершённому `move_quietly.slot`, включая полный `ActionExecutionReceipt`. Отсутствующий/незавершённый slot, другое действие либо подмена receipt ID/source/result/rule отклоняются до RNG во всех wrappers. Сравнение структурное: равная копия slot/receipt допустима. В более позднем round прежний receipt не требуется и первый slot может содержать другое действие. Это проверка provenance, а не новое игровое условие; основание остаётся BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117. `test_k1_hidden_attack_chronology.py` теперь содержит 12 тестов порядка и source-slot binding.

## Actor-scoped hidden lifecycle

`HiddenLifecycleState` объединяет `hiding_positions: HidingPositionState`, optional `active_move_quietly` и ordered `consumed_opportunity_ids`. Actor выводится из вложенного history state, opportunity — из exact active result; копий этих данных нет. Active result обязан быть успешным HIDDEN того же actor, соответствовать текущей истории укрытий и не входить в consumed IDs. Изменение round не сбрасывает состояние.

`HiddenLifecycleApplicationRequest → Result` / `apply_hidden_lifecycle_result` принимает закрытый набор completed results:

- Move Quietly принимается только при отсутствии активной opportunity: HIDDEN активирует source, FAILED и SUCCEEDED_WITHOUT_HIDING сохраняют тот же inactive state; actor/history/rule и consumed source проверяются для всех outcomes;
- continuation `PRESERVED` возвращает тот же state и допускает идемпотентный повтор; `LOST` очищает active source и дописывает consumed opportunity ID, сохраняя историю атакованных укрытий;
- completed Give Ground loss очищает active source без повторного движения/Condition application и изменения истории; same-round требует точный post-hiding snapshot либо подтверждённую movement chain;
- completed free-movement loss очищает active source после доказанного ухода в другую Zone без нового receipt и изменения истории;
- standalone loss очищает active source и принимает consumed opportunity IDs из existing loss result без изменения HidingPositionState;
- registered Attack очищает active source, принимает точную consumption chain и обновлённый HidingPositionState из nested result; повторное применение либо реактивация использованного source с новым application ID отклоняются.

Source-bound application проверяет полное равенство активного Move Quietly result, а не только совпадение ID. `continue_hidden_lifecycle(state, request)` сверяет source/chain до существующего continuation reducer; `execute_hidden_lifecycle_attack(state, request, rng, decisions=...)` дополнительно сверяет полный HidingPositionState до RNG, затем вызывает registered executor один раз и возвращает согласованный lifecycle result. Поддерживаются все три registered Attack ветви. Result сохраняет completed result, previous/current state и ordered trace. Следующая Move Quietly использует `prepare_move_quietly_with_hiding_positions(state.hiding_positions, request)`.

Caller хранит один актуальный snapshot; откат к старому snapshot или конкурентное применение чистые функции не обнаруживают. При исключениях входные состояния неизменны, RNG и decision-provider effects не откатываются. Aim/weapon/injury результаты остаются во вложенном execution; lifecycle не владеет их storage и не вычисляет awareness. Failed/declined Move Quietly принимается как completed action result, но не активирует opportunity. Замена ещё активной opportunity новой пока не подключена; это граница API, не новый запрет книги. Перед новой активацией caller должен завершить старую через поддержанный переход, а не молча потерять её.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre и Attack Tests, стр. 117–118. Rule ID: `RULE-COMBAT-014:hidden-lifecycle`. Код: `hidden_lifecycle_models.py`, `hidden_lifecycle_resolution.py`; тесты: `test_k1_hidden_lifecycle_resolution.py` (14 детерминированных тестов).

`lose_hidden_lifecycle_opportunity(state, request)` проверяет exact active Move Quietly source и ordered consumed chain до единственного вызова `lose_move_quietly_hidden_attack`. Existing `LEFT_HIDING_POSITION`, `DIFFERENT_TARGET` и `TARGET_AWARE` сохраняются без новых условий; результат атомарно очищает active source и переносит consumption chain. Уже полученный `MoveQuietlyHiddenAttackLossResult` можно передать напрямую в `HiddenLifecycleApplicationRequest`, с теми же проверками source/chain, без повторного reducer. HidingPositionState сохраняется целиком, включая used positions и attack execution IDs; новый receipt, Attack, регистрация укрытия или RNG не создаются. Loss context описывает неприменимость opportunity и сам не доказывает исполнение названного next action. Stationary non-Attack остаётся continuation path, eligible hidden Attack — execution path. Тесты: `test_k1_hidden_lifecycle_loss_resolution.py` (7 тестов).

`execute_hidden_lifecycle_move_quietly(state, request, rng, decisions=...)` закрывает исполнение Move Quietly и применение его результата одним immutable переходом. До executor/RNG проверяются inactive lifecycle, actor, известный Rule ID, точное совпадение ordered used-position history и отсутствие `request.id:hidden` в consumed opportunities. История не подменяется молча: подготовить request можно через существующий `prepare_move_quietly_with_hiding_positions`.

Исполняется один existing Move Quietly executor с тем же TestDecisionProvider, одной opposed Test и одним receipt. Result сохраняет action result со всеми turn/spatial transitions и trace. HIDDEN включает conditional movement и активирует exact source; оба non-hidden outcomes оставляют тот же lifecycle state/history/consumed IDs. После любого outcome caller обязан сохранять также возвращённый action round/spatial state: при failure/decline защита от повторного броска опирается на completed action slot, а не на новую фиктивную opportunity. Повторное применение самого non-hidden результата к тому же inactive snapshot идемпотентно; вызов с заведомо старым turn snapshot чистые функции не выявляют. Исключения не возвращают частичных snapshots, RNG не откатывается. Active-source replacement не добавлен. Тесты: `test_k1_move_quietly_hidden_lifecycle.py` (9 тестов), сквозной two-cycle тест `test_k1_hidden_lifecycle_resolution.py` теперь использует этот adapter на обеих Move Quietly фазах.


`HiddenFreeMovementLossRequest → Result` связывает completed `FreeMovementResult` с успешной Move Quietly. `lose_hidden_opportunity_after_free_movement` возвращает `LEFT_HIDING_POSITION`, дописывая opportunity ID ровно один раз. `apply_hidden_lifecycle_free_movement(state, request)` сначала проверяет exact active source и ordered consumption chain, затем один раз вызывает consumer и очищает active source. Готовый loss result также принимается общим lifecycle application без повторного consumer. История used hiding positions и attack execution IDs сохраняется целиком: уход не является скрытой атакой.

Проверяются actor, исходный placement (включая side), тот же Zone graph, известные source Rule IDs и более поздний round. FreeMovementResult уже доказывает непустой маршрут в другую Zone и точный spatial transition. Move Quietly расходует free move и при route hiding, и при укрытии в текущей Zone, поэтому второй free move в том же round отклоняется даже при подмене usage snapshot; сам переход к следующему round opportunity не гасит. Изменения чужих placements и round-scoped usage между source и движением разрешены. Same-Zone уход этот контракт не описывает.

Consumer не исполняет движение, Attack или action slot, не создаёт receipt, не использует RNG и не меняет awareness/Aim. Сохраняются исходный completed movement и его trace. Caller хранит актуальный lifecycle вместе с `movement.state` и `movement.round_state`: произвольный откат snapshots или пропущенный промежуточный уход с возвратом чистый consumer не обнаруживает. Автоматического battle aggregate и active-source replacement нет.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions, стр. 116 (один free move); Manoeuvre, стр. 117 (атака из исходной позиции); Attack Tests, стр. 118 (новое укрытие после атаки). Код: `hidden_movement_models.py`, `hidden_movement_resolution.py`, lifecycle adapter. Тесты: `test_k1_hidden_free_movement_resolution.py` — 10 детерминированных тестов.


`HiddenFreeMovementExecutionRequest` принимает исходную успешную Move Quietly, `FreeMovementRequest` и ordered consumed opportunity IDs. Общий с completed-loss контрактом validator проверяет source, actor, placement/graph, более поздний round, Rule ID и непогашенную opportunity при создании запроса. `execute_hidden_lifecycle_free_movement(state, request)` до движения сверяет exact active source и consumption chain, затем вызывает один `resolve_free_movement` и один existing loss consumer через `apply_hidden_lifecycle_free_movement`. Ограничения Prone/Defenceless, usage, маршрута, obstacle/enemy blockers и Difficult Terrain остаются у общего movement resolver.

Возвращается существующий `HiddenLifecycleApplicationResult`: `state` содержит погашенную opportunity и прежний HidingPositionState, а `completed.source_request.movement` — единственный completed movement со spatial/round snapshots и trace. Дополнительного result типа, receipt, RNG или Attack нет. При ошибке любой стадии частичный результат не возвращается и входные snapshots сохраняются. Replay с новыми wrapper/movement IDs отклоняется по актуальному lifecycle до движения. Caller по-прежнему хранит оба возвращённых состояния; откат к старым snapshots автоматически не обнаруживается. Семантика Aim, same-Zone границы и отсутствие awareness/replacement остаются прежними.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions и Manoeuvre, стр. 116–117; Attack Tests, стр. 118. Тесты: `test_k1_hidden_free_movement_execution.py` — 8 тестов, включая Normal/Fast, preflight, movement failures, ошибки всех стадий, replay и сохранность Aim receipt; прежние 10 consumer-тестов проверяют общий preflight после рефакторинга.


`HiddenGiveGroundLossRequest → Result` принимает успешную Move Quietly и готовый `GiveGroundResolutionResult`. `lose_hidden_opportunity_after_give_ground` погашает opportunity с `LEFT_HIDING_POSITION`; `apply_hidden_lifecycle_give_ground` перед consumer проверяет exact active source и ordered consumption chain и возвращает прежний `HiddenLifecycleApplicationResult`. Готовый loss result также можно применить напрямую без повторного consumer. HidingPositionState, включая used positions и Attack execution IDs, сохраняется целиком.

Проверяются владелец, исходный hidden placement (включая side), тот же graph, round не раньше Move Quietly и trace общего Give Ground resolver. Его source Rule ID сохраняется без ограничения конкретной причиной Give Ground. В том же round без промежуточной цепочки требуется структурно точное равенство `movement.previous_state` и `move_quietly.spatial_state`: так принимается прямое продолжение после скрытия, включая same-Zone hiding, где изменился только free-move usage. Голое совпадение позиции недостаточно. Более поздний round допускает изменения чужих placements и round-scoped usage при сохранении placement владельца и graph. Give Ground имеет собственный лимит; расход free move не запрещает same-round Give Ground.

Same-round путь с промежуточными spatial changes принимает только подтверждённую цепочку completed FreeMovementResult/GiveGroundResolutionResult других actor; прочие изменения пока не поддержаны. Это граница API, не запрет книги; caller не должен подменять previous_state готового движения ради обхода проверки. Автоматическая проверка полной истории и отката snapshots отсутствует. Consumer сохраняет исходный movement, его Conditions/Broken и trace без повторного движения или Condition application; не создаёт Attack, action receipt, RNG, awareness или регистрацию позиции. Caller сохраняет spatial/Condition/lifecycle результаты вместе.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117 (атака из исходной позиции); Giving Ground, стр. 119 (соседняя Zone, собственный round limit, Broken). Код: `hidden_give_ground_models.py`, `hidden_give_ground_resolution.py`, lifecycle adapter. Тесты: `test_k1_hidden_give_ground_resolution.py` — 10 детерминированных тестов same/later round, exact snapshot, чужого actor/source/graph, stale/replay, history, trace, Broken и ошибок.


`HiddenGiveGroundExecutionRequest` связывает успешную Move Quietly, `GiveGroundResolutionRequest` и ordered consumed opportunity IDs. Общий с completed-loss request preflight проверяет source, actor, исходный placement/graph, chronology и непогашенную opportunity. `execute_hidden_lifecycle_give_ground(state, request)` перед движением сверяет exact active source и chain, один раз вызывает `resolve_give_ground`, затем один раз прежний loss consumer через `apply_hidden_lifecycle_give_ground`.

Результат — существующий `HiddenLifecycleApplicationResult`: `completed.source_request.movement` хранит единственный GiveGroundResolutionResult со spatial/Conditions/Broken/source/trace. Caller сохраняет эти результаты вместе с lifecycle. Общий resolver сохраняет ограничения Prone/Defenceless, once-per-round, направления от attacker, adjacency, enemy blockers, obstacles и Difficult Terrain; source Rule ID причины Give Ground не ограничивается новым whitelist. При входе во вражескую Zone Condition application происходит ровно один раз. Новый RNG, Attack, action receipt или регистрация позиции не создаются. Ошибка любой стадии не возвращает частичные snapshots и не мутирует входы. Replay с новыми IDs отклоняется по актуальному lifecycle до движения. Same-round путь принимает exact post-hiding snapshot либо описанную ниже непрерывную цепочку двух поддержанных типов движения.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117; Giving Ground, стр. 119. Тесты: `test_k1_hidden_give_ground_execution.py` — 8 тестов same/later round, одного movement/Condition/loss, preflight, movement restrictions, исключений всех стадий, replay, trace и сохранности истории; прежние 10 consumer-тестов проверяют общий validator после рефакторинга.


Оба Give Ground hidden request принимают optional immutable `intervening_movements`: tuple завершённых `FreeMovementResult | GiveGroundResolutionResult`. Общий validator проверяет один round исходной Move Quietly, точное начало от её post-hiding spatial snapshot, равенство каждого previous_state предыдущему state и точное завершение на snapshot перед Give Ground владельца. Graph и placement владельца должны сохраняться на каждом шаге; сам владелец не может быть mover промежуточного перехода, даже если конечная позиция восстановлена. При пустом tuple прежняя exact-snapshot ветвь сохранена; non-empty chain для более позднего round отклоняется, прежний later-round путь остаётся доступен без chain.

Разрывы, перестановка, дубли шагов, неполная цепочка, неизвестные типы/Rule IDs, другой graph/round и stale lifecycle отклоняются до owner movement. Неподдержанные виды пространственных изменений не восстанавливаются предположениями. Chain хранится в completed loss source, а её ordered Rule IDs входят в общий trace. Executor передаёт те же completed results consumer без повторного движения, Condition application, RNG или расхода чужих opportunity. Их Conditions/Broken и turn snapshots остаются историческими результатами; актуальное хранение принадлежит caller. Это проверка spatial continuity, не автоматическая awareness или полная история боя.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117; Giving Ground, стр. 119. Книжные правила не изменены. Тесты: `test_k1_hidden_give_ground_movement_chain.py` — 8 тестов всех сочетаний двух типов, continuity/replay, отказа при уходе владельца, trace, normalization и исключений.


Сквозная проверка `tests/integration/test_k1_hidden_recovery_cycle.py` связывает публичные adapters: Move Quietly → completed free move scout → Give Ground владельца во вражескую Zone/Broken → следующий round, уход в свободную Zone и Recover/Willpower → новая Move Quietly → Aim → prepared hidden Crossbow Attack в следующем round. Scheduler завершает стандартные действия всех участников и явно продвигает rounds; дополнительные actions не выдаются. Причина исходного Give Ground передаётся внешним GiveGroundRequest, awareness/cover факты остаются явными — полного battle loop тест не создаёт.

Два исходных теста охватывают hit/miss × нулевой/положительный Aim и отдельный failure Recover. Unsafe Recover отклоняется до RNG; failure сохраняет Broken и завершает action slot. Spatial/Condition/hidden consumption/history snapshots передаются без ручного снятия Broken или восстановления opportunity. От первой скрытой позиции не появляется used-position запись после Give Ground; только завершённый выстрел регистрирует вторую позицию, расходует Aim и hidden opportunity и разряжает Crossbow с новым reload cycle. Повтор и реактивация отклоняются, входные состояния неизменны. Производственные API и правила не менялись.

Источники: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions / Aim, стр. 116; Manoeuvre, стр. 117; Attack Tests, стр. 118; Giving Ground, стр. 119; Conditions / Broken, стр. 122; Equipment / Ranged Weapons, стр. 95 (Crossbow).

Продолжение того же сценария проверяет Reload и второй скрытый Crossbow выстрел: возвращённый unloaded state запрещает подготовку атаки до completion; три стандартных Dexterity actions накапливают 0 → 1 → 2 успеха, сохраняя weapon instance и первый reload cycle. Новый Move Quietly request получает actor-scoped историю, отклоняет прежнюю позицию и активирует новое укрытие. Второй hit/miss расходует только новую hidden opportunity, сохраняет уже погашённую Aim chain, регистрирует новый Attack ID/позицию и создаёт второй unloaded cycle с нулевым progress. Исходные snapshots и receipt replay guards проверены. Продолжение начинается с первого промаха, чтобы промежуточные idle Recover не требовали отдельного переноса Conditions цели; первый hit проверяется прежним сценарием. Этот этап добавил третий тест модуля. Источники: BOOK-PLAYER-GUIDE 1.4, Equipment / Ranged Weapons, стр. 94–95; Rules / Exacting Tests, стр. 110; Attack Tests, стр. 118. Новых игровых API нет.

После завершённого Reload дополнительно проверена последовательность FAILED Move Quietly → SUCCEEDED_WITHOUT_HIDING (явный DECLINE) → HIDDEN → prepared hidden Attack. Первые две попытки завершают по одному стандартному action slot в разных rounds, оставляют исходный inactive lifecycle и spatial snapshot, не расходуют free move, не создают opportunity и не добавляют записи в историю. Повторное исполнение с возвращённым round state отклоняется до RNG. Загруженное оружие и уже погашённая Aim chain передаются последующему выстрелу; успешное скрытие использует ту же позицию, на которой ранее провалилась проверка. Проверены оба исхода второго выстрела, два reload cycle, trace и неизменяемость входов. Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Manoeuvre, стр. 117; Attack Tests, стр. 118. Этот этап добавил четвёртый тест модуля; production API не менялись.

Пятый тест hidden recovery cycle проверяет другой порядок: первый Crossbow выстрел → новое укрытие с unloaded weapon state → три Reload actions с накоплением 0 → 1 → 2 → prepared hidden Attack. Каждый completed Reload receipt передаётся continuation с неизменной позицией и явным position_revealed=False; возвращаются PRESERVED, тот же active Move Quietly source/opportunity и неизменные history/consumption chains. Reload progress и action slots сохраняются независимо. Финальный hit/miss исполняется один раз, создаёт один receipt, расходует opportunity и регистрирует позицию, сохраняя прежнюю Aim chain и создавая второй reload cycle. Проверены trace, replay и неизменяемость; awareness не вычисляется. Источники: BOOK-PLAYER-GUIDE 1.4, Equipment / Ranged Weapons, стр. 94–95; Rules / Manoeuvre, стр. 117; Attack Tests, стр. 118. Production API не менялись.

Шестой тест hidden recovery cycle передаёт position_revealed=True после частичного Reload (1 из 2 успехов). Continuation возвращает LOST/POSITION_REVEALED, единожды погашает opportunity и очищает active source без добавления used hiding position или Attack execution ID. Повтор continuation отклоняется; completed Reload result остаётся неизменным. Следующее действие доводит тот же progress до 2 и заряжает Crossbow. Подготовленная атака со старым source/старой chain отклоняется lifecycle до executor/RNG; request с актуальной consumed chain также отклоняется как уже погашенный. Затем новая Move Quietly в другой позиции и второй hit/miss сохраняют всю цепочку потерь, регистрируя только новое укрытие выстрела. Причина раскрытия передаётся внешним фактом, а не выводится из Reload. Источники: BOOK-PLAYER-GUIDE 1.4, Equipment / Ranged Weapons, стр. 94–95; Rules / Manoeuvre, стр. 117; Attack Tests, стр. 118. Новых production API и правил нет.

Седьмой тест hidden recovery cycle проверяет Move Quietly → Aim → Reload → prepared hidden Attack (Aim 0/2 успеха × финальный hit/miss). Первый завершённый Reload, даже с нулевым вкладом, выбирается как следующее действие Aim: AimFollowUp получает фактические actor/action ID/declaration из его receipt и возвращает LOST без attack/modifier. Continuation по Aim и каждому Reload сохраняет тот же hidden source/opportunity при неизменной позиции без раскрытия. После Reload 0 → 1 → 2 prepared Attack не получает Aim modifier, тратит только базовые два куба и однократно расходует hidden opportunity/регистрирует позицию. Готовый LOST отвергается Aim-bound Attack request. Учёт LOST и добавление source/follow-up IDs теперь выполняет отдельный consumer, описанный ниже; автоматического Aim lifecycle ещё нет. Источники: BOOK-PLAYER-GUIDE 1.4, Rules / Aim, стр. 116; Manoeuvre, стр. 117; Attack Tests, стр. 118; Equipment / Ranged Weapons, стр. 94–95. Production API не менялись.

### Учёт потери Aim после завершённого неатакующего действия

`AimConsumptionState` хранит actor и отдельные ordered unique `consumed_aim_source_ids`/`consumed_aim_follow_up_ids`; префиксы могут импортироваться независимо из прежних consumers. `AimLossConsumptionRequest` связывает готовый LOST с completed неатакующим receipt: проверяет канонические Rule IDs, actor, exact action ID/declaration и хронологию. Receipt должен быть позже Aim: второй slot того же turn либо первый slot более позднего round. Во втором slot позднего turn уже было другое действие; expiry по числу rounds не вводится. `consume_lost_aim` возвращает `AimLossConsumptionResult` с прежним/новым state, source и полным trace; добавляет исходный Aim request ID и follow-up ID ровно один раз. Повтор source отвергается и с новым follow-up ID. Нет RNG, нового receipt или повторного исполнения Aim/следующего действия. Caller выбирает фактически следующее действие и сохраняет актуальную историю; receipt-only контракт не доказывает отсутствие действий в промежуточных rounds. APPLIED регистрируется отдельным consumer завершённой Aim-bound ranged Attack. Потери при атакующих действиях и автоматический Aim dispatcher пока вне среза. Для проверки истории при подготовке предусмотрен prepare_ranged_weapon_attack_with_aim_history; прежняя низкоуровневая preparation остаётся доступной без history. Caller обязан использовать adapter и актуальный snapshot.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions / Aim, стр. 116. Правило Aim не изменено. Публичные модели: `aim_consumption_models.py`; reducer: `aim_consumption_resolution.py`. Десять тестов `test_k1_aim_consumption_resolution.py` проверяют history/replay, Aim 0/2, actor/action binding, chronology, unsupported outcomes, immutable snapshots, типы/provenance/trace. Интеграция Aim → Reload → hidden Attack теперь передаёт chain из результата consumer.

### Подготовка ranged Attack с историей Aim

`prepare_ranged_weapon_attack_with_aim_history(state, request)` принимает AimConsumptionState и существующий RangedWeaponAttackPreparationRequest. До вызова прежней preparation проверяет типы, совпадение history actor с Attack actor и отсутствие optional Aim request ID в consumed_aim_source_ids. Новые preparation/Attack/follow-up IDs не обходят запрет source. После проверки ровно один раз делегирует prepare_ranged_weapon_attack и возвращает тот же тип результата с прежними source/profile/trace. Без Aim и со свежим Aim (включая нулевой bonus) поведение сохраняется. Подготовка не использует RNG, не исполняет Attack и не меняет историю; регистрация APPLIED доступна отдельным consumer готового исполнения. Caller хранит актуальный snapshot и передаёт consumed_aim_follow_up_ids в prepared executor. Прежняя низкоуровневая preparation не принимает history; прямой обход adapter или откат snapshot не предотвращаются.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Aim, стр. 116. Шесть тестов test_k1_ranged_preparation_aim_history.py проверяют free/Crossbow, Aim 0/1, no-Aim, consumed source с новыми IDs до delegate, actor/types, immutable snapshots и исключение preparation. Интеграция Aim → Reload → hidden Attack передаёт returned loss history: прежний Aim отклоняется до подготовки бонуса, а hit/miss без Aim сохраняет обе consumption chains.

### Регистрация завершённой Aim-bound ranged Attack

`AimAttackConsumptionRequest` принимает AimConsumptionState и готовый AimRangedWeaponAttackExecutionResult. Проверяются actor, канонические Rule IDs, APPLIED follow-up, точная связь подготовленной Attack/receipt и хронология относительно Aim; вложенные typed results проверяют kernel/turn transition. Предыдущий consumed follow-up prefix должен точно совпадать с history, включая порядок. `register_aim_ranged_attack` возвращает AimAttackConsumptionResult с source, прежним/новым state и trace: добавляет исходный Aim request ID, переносит готовую consumed follow-up chain без повторного append. Hit/miss и Aim с нулевым bonus погашаются одинаково. Повтор source, включая новые Attack/execution/follow-up/registration IDs, отклоняется; LOST source также нельзя повторно зарегистрировать как APPLIED. Нет RNG, переисполнения Attack, нового receipt или изменения weapon/hidden snapshots. Caller сохраняет актуальный state и передаёт его следующей history-aware preparation; consumer готового результата сам по себе не предотвращает исполнение устаревшего request до регистрации. Atomic execution+registration реализован отдельным adapter ниже; LOST от атакующих действий, автоматический next-action dispatcher и battle aggregate не добавлены.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Aim, стр. 116. Модели и consumer находятся в aim_consumption_models.py и aim_consumption_resolution.py. Девять unit-тестов test_k1_aim_attack_consumption.py проверяют free/Crossbow × hit/miss × Aim 0/2, replay/LOST, actor/prefix, chronology, вложенные receipt/source, immutable provenance/trace и передачу history следующей preparation. Hidden recovery cycle регистрирует первый completed Aim-bound result и переносит полученную историю в следующий LOST consumer.

### Атомарное исполнение Aim-bound ranged Attack с регистрацией

`RegisteredAimRangedAttackExecutionRequest` связывает AimConsumptionState и существующий AimRangedWeaponAttackExecutionRequest. Общий с consumer готового результата preflight проверяет actor, canonical source, APPLIED, точную Attack, previous follow-up prefix и chronology до RNG. `execute_registered_aim_ranged_attack` один раз вызывает execute_aim_ranged_weapon_attack и один раз register_aim_ranged_attack. Frozen/slotted RegisteredAimRangedAttackExecutionResult хранит source request, единственную registration и точную trace; execution и state доступны как views вложенного результата, без копии weapon/Attack/receipt. Смена wrapper/Attack/execution/follow-up IDs не позволяет повторно использовать погашенный Aim при актуальной history. Hit/miss и Aim 0 регистрируются одинаково.

Исключение не изменяет входные snapshots; уже использованные RNG и решения provider не откатываются. Caller выбирает фактически следующее действие и сохраняет актуальную history. Prepared Aim ветвь подключена отдельным атомарным adapter ниже; совместная hidden/Aim registration доступна отдельным adapter ниже; низкоуровневый обход и откат history не предотвращаются. LOST от атакующего действия, automatic awareness и battle aggregate не добавлены.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions / Aim, стр. 116, непосредственно сверено по локальному pages/0116.txt. Новых игровых правил нет. Девять тестов test_k1_registered_aim_ranged_attack.py проверяют free/Crossbow × hit/miss × Aim 0/2, один executor/kernel/receipt/registration, replay до RNG, actor/prefix/chronology, исключения и immutable provenance/trace.

### Атомарное исполнение prepared ranged Attack с Aim history

`RegisteredPreparedAimRangedAttackExecutionRequest` принимает AimConsumptionState и существующий PreparedRangedWeaponAttackExecutionRequest с обязательным Aim follow-up. Через точный вложенный Aim request общий preflight проверяет actor/source/prefix/chronology до RNG. No-Aim request отклоняется; прежний prepared API остаётся доступен для этой ветви.

`execute_registered_prepared_aim_ranged_attack` ровно один раз вызывает execute_prepared_ranged_weapon_attack и передаёт его единственный вложенный Aim-bound result в register_aim_ranged_attack. Frozen/slotted RegisteredPreparedAimRangedAttackExecutionResult связывает source request, prepared execution и registration; state доступен как view регистрации. Проверяются совпадение prepared source, history и вложенного Aim execution, а также точная trace с preparation/profile rules. Второго kernel, receipt, применения bonus или добавления follow-up ID нет. Свободное оружие сохраняет свой state, Crossbow открывает один следующий reload cycle; hit/miss и Aim 0 регистрируются одинаково. Возвращённая history блокирует повтор источника даже при новых preparation/Attack/follow-up/wrapper IDs.

Источник: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions / Aim, стр. 116, непосредственно прочитан локальный pages/0116.txt. Девять тестов test_k1_registered_prepared_aim_ranged_attack.py проверяют free/Crossbow × hit/miss × Aim 0/2, единственность исполнения/регистрации, trace/reload, replay и guards до RNG, chronology, no-Aim/types, исключения и неизменность входов. RNG/decision effects при ошибке не откатываются. Caller сохраняет актуальную history и выбирает next action; совместная hidden/Aim registration доступна отдельным adapter ниже, battle aggregate и automatic awareness не добавлены.

### Совместная регистрация Aim и раскрытого укрытия

`RegisteredHiddenAimAttackExecutionRequest` содержит AimConsumptionState и существующий RegisteredHiddenAttackExecutionRequest только с PreparedHiddenRangedAttackExecutionRequest и обязательным Aim follow-up. Общий preflight проверяет hiding-position history и Aim actor/source/follow-up prefix/chronology до RNG; вложенные requests сохраняют exact Attack/opportunity/source/placement binding. Ordinary/profile-aware hidden и prepared no-Aim не входят в этот adapter.

`execute_registered_hidden_aim_attack` один раз вызывает execute_registered_hidden_attack и один раз register_aim_ranged_attack для его готового nested Aim execution. Единственная атака расходует hidden opportunity, регистрирует used hiding position и погашает Aim source/follow-up. Hit/miss и Aim 0 обрабатываются одинаково. RegisteredHiddenAimAttackExecutionResult хранит source, registered hidden result, связанную Aim registration и точную объединённую trace. execution, aim_state и hiding_position_state — views вложенных результатов; weapon state, profile trace и receipt не копируются. Обе регистрации относятся к одной атаке. Повтор источника Aim или позиции/Attack отклоняется даже при новых внешних IDs; caller передаёт актуальные histories и consumed opportunity prefix.

При ошибке входные snapshots неизменны, использованные RNG/decision effects не откатываются. HiddenLifecycleState пока не объединён с этим adapter; актуальный source и непрерывность opportunity history хранит caller. Автоматический выбор next action, awareness и battle aggregate не добавлены. Публичные модули: registered_hidden_aim_models.py и registered_hidden_aim_resolution.py. Десять тестов test_k1_registered_hidden_aim_attack.py проверяют free/Crossbow × hit/miss × Aim 0/2, один kernel/receipt и каждую регистрацию, обе истории и profile trace, replay/guards до RNG, типы/provenance/trace, ошибки и передачу возвращённых histories следующей подготовке.

Источники непосредственно сверены: BOOK-PLAYER-GUIDE 1.4, Rules / Combat Actions / Aim, стр. 116; Manoeuvre / Move Quietly, стр. 117; Attack Tests, стр. 118 (локальные pages/0116.txt–0118.txt). Игровые правила не изменены.
